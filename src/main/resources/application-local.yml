spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/pulseops
    username: pulseops
    password: pulseops

  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
